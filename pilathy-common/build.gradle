dependencies {

    // Commons-io
    api("commons-io:commons-io:${commonsIoVersion}")

    // TestFixtures
    testImplementation(testFixtures(project(":pilathy-common")))
}

bootJar {
    enabled = false
}
jar {
    enabled = true
}
